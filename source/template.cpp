#include <bits/stdc++.h>
using namespace std;

#define all(c) begin(c),end(c)
#define ssize(c) (int)c.size()

using ll = long long;
using vi = vector<int>;
using vll = vector<ll>;

int main() {
	cin.tie(0)->sync_with_stdio(false);
}