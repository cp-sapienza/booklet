#pragma once

#include <bits/stdc++.h>
using namespace std;

#define all(c) begin(c),end(c)
#define ssize(c) (int)c.size()

using ll = long long;
using ld = long double;
using vi = vector<int>;
using vll = vector<ll>;
