#include <bits/stdc++.h>
using namespace std;

int32_t main() {
    // generator input
    int input; cin >> input;

    // solution output
    int ans; cin >> ans;
    cout << (ans == input ? "OK" : "il numero e' sbagliato") << endl;
}
